---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const servicios = [
  'Escrituras y Protocolos',
  'Poderes Notariales',
  'Testamentos',
  'Actas Notariales',
  'Certificaciones',
  'Asesoría Jurídica',
];

const navLinks = [
  { href: `${base}/`, label: 'Inicio' },
  { href: `${base}/blog`, label: 'Blog' },
  { href: `${base}/contacto`, label: 'Contacto' },
];

const currentPath = Astro.url.pathname;
---

<header class="header" id="header">
  <div class="header-inner">
    <a href={`${base}/`} class="logo" aria-label="Notaría 56 - Inicio">
      <img
        src={`${base}/images/notaria56-new.png`}
        alt="Notaría 56"
        class="logo-img"
        width="1600"
        height="900"
      />
    </a>

    <nav class="nav" id="nav">
      <a href={`${base}/`} class={`nav-link ${currentPath === base + '/' || currentPath === base ? 'active' : ''}`}>
        Inicio
      </a>

      <div class="nav-dropdown">
        <a href={`${base}/#servicios`} class="nav-link nav-dropdown-trigger">
          Servicios <span class="chevron">▾</span>
        </a>
        <ul class="dropdown-menu">
          {servicios.map(s => (
            <li>
              <a href={`${base}/#servicios`}>{s}</a>
            </li>
          ))}
        </ul>
      </div>

      <a href={`${base}/#nosotros`} class="nav-link">Nosotros</a>

      {navLinks.slice(1).map(link => (
        <a
          href={link.href}
          class={`nav-link ${currentPath === link.href ? 'active' : ''}`}
        >
          {link.label}
        </a>
      ))}
    </nav>

    <button class="menu-toggle" id="menuToggle" aria-label="Abrir menú">
      <span></span><span></span><span></span>
    </button>
  </div>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: var(--color-navy);
    box-shadow: 0 2px 16px rgba(0,0,0,0.30);
    transform: translateY(-100%);
    transition: transform 0.35s ease;
  }

  .header.visible {
    transform: translateY(0);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 5rem;
    gap: 1rem;
    width: 100%;
    padding: 0 1.5rem 0 1rem;
  }

  .logo {
    display: flex;
    align-items: center;
    text-decoration: none;
    flex-shrink: 0;
  }

  .logo-img {
    height: 4.6rem;
    width: auto;
    display: block;
    image-rendering: -webkit-optimize-contrast;
  }

  /* Nav */
  .nav {
    display: flex;
    gap: 0;
    align-items: center;
  }

  .nav-link {
    color: rgba(255,255,255,0.82);
    font-family: 'Montserrat', 'Helvetica Neue', sans-serif;
    font-weight: 300;
    font-size: 0.85rem;
    padding: 0.45rem 0.95rem;
    border-radius: 2px;
    transition: color 0.2s;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .nav-link:hover,
  .nav-link.active {
    color: var(--color-gold-pale);
  }

  /* Dropdown */
  .nav-dropdown {
    position: relative;
  }

  .nav-dropdown-trigger {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    cursor: pointer;
  }

  .chevron {
    font-size: 0.65rem;
    transition: transform 0.2s;
    display: inline-block;
  }

  .nav-dropdown:hover .chevron {
    transform: rotate(180deg);
  }

  .dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    background: var(--color-navy);
    border: 1px solid rgba(158,124,63,0.25);
    border-top: 2px solid var(--color-gold);
    list-style: none;
    min-width: 240px;
    border-radius: 0 0 3px 3px;
    box-shadow: 0 8px 24px rgba(0,0,0,0.35);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-6px);
    transition: opacity 0.2s, transform 0.2s, visibility 0.2s;
    z-index: 200;
    padding-top: 0;
  }

  .nav-dropdown:hover .dropdown-menu,
  .nav-dropdown:focus-within .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown-menu li a {
    display: block;
    padding: 0.6rem 1.1rem;
    color: rgba(255,255,255,0.78);
    font-family: 'Montserrat', 'Helvetica Neue', sans-serif;
    font-weight: 300;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    transition: color 0.15s, background 0.15s;
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }

  .dropdown-menu li:last-child a {
    border-bottom: none;
  }

  .dropdown-menu li a:hover {
    color: var(--color-gold-pale);
    background: rgba(158,124,63,0.08);
  }

  /* Mobile toggle */
  .menu-toggle {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.25rem;
  }

  .menu-toggle span {
    display: block;
    width: 22px;
    height: 2px;
    background: var(--color-white);
    transition: all 0.3s;
  }

  @media (max-width: 820px) {
    .header {
      transform: translateY(0);
    }

    .menu-toggle {
      display: flex;
    }

    .nav {
      display: none;
      position: absolute;
      top: 5rem;
      left: 0;
      right: 0;
      background: var(--color-navy);
      flex-direction: column;
      padding: 0.5rem 0 1rem;
      gap: 0;
      box-shadow: 0 8px 20px rgba(0,0,0,0.3);
    }

    .nav.open {
      display: flex;
    }

    .nav-link {
      padding: 0.85rem 1.5rem;
      border-bottom: 1px solid rgba(255,255,255,0.06);
      width: 100%;
    }

    .nav-dropdown {
      width: 100%;
    }

    .nav-dropdown-trigger {
      width: 100%;
      justify-content: space-between;
    }

    .dropdown-menu {
      position: static;
      opacity: 1;
      visibility: visible;
      transform: none;
      box-shadow: none;
      border: none;
      border-top: none;
      background: rgba(0,0,0,0.25);
      display: none;
    }

    .nav-dropdown.mobile-open .dropdown-menu {
      display: block;
    }

    .nav-dropdown.mobile-open .chevron {
      transform: rotate(180deg);
    }

    .dropdown-menu li a {
      padding-left: 2.5rem;
    }
  }
</style>

<script>
  const header = document.getElementById('header');
  const toggle = document.getElementById('menuToggle');
  const nav = document.getElementById('nav');

  toggle?.addEventListener('click', () => {
    nav?.classList.toggle('open');
  });

  document.querySelectorAll('.nav-dropdown-trigger').forEach(trigger => {
    trigger.addEventListener('click', (e) => {
      if (window.innerWidth <= 820) {
        e.preventDefault();
        trigger.closest('.nav-dropdown')?.classList.toggle('mobile-open');
      }
    });
  });

  // Hover-reveal navbar (desktop only)
  if (header) {
    const SHOW_BAND = 70;
    const HIDE_DELAY = 220;
    let hideTimer: number | undefined;

    const isDesktop = () => window.innerWidth > 820;

    const show = () => {
      if (!isDesktop()) return;
      if (hideTimer) {
        clearTimeout(hideTimer);
        hideTimer = undefined;
      }
      header.classList.add('visible');
    };

    const scheduleHide = () => {
      if (!isDesktop()) return;
      if (hideTimer) clearTimeout(hideTimer);
      hideTimer = window.setTimeout(() => {
        header.classList.remove('visible');
      }, HIDE_DELAY);
    };

    document.addEventListener('mousemove', (e) => {
      if (!isDesktop()) return;
      if (e.clientY <= SHOW_BAND) show();
    });

    header.addEventListener('mouseenter', show);
    header.addEventListener('mouseleave', scheduleHide);

    // Brief peek on first load so the user discovers it exists
    if (isDesktop()) {
      header.classList.add('visible');
      window.setTimeout(() => {
        if (!header.matches(':hover')) header.classList.remove('visible');
      }, 1800);
    }
  }
</script>
